[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    unit: fast deterministic unit tests
    contract: OpenAPI/API contract tests
    integration: DB-backed integration tests
    e2e: end-to-end tests
    regression: bug regression coverage
    prompt: prompt/golden-output regression coverage
